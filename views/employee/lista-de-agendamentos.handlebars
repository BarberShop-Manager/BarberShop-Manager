<main>
    <div class="container">
        <div id="calendar"></div>
    </div>
{{#each cadhorario}}
    <div class="card">
        <h2>Todos Agendamentos</h2>
        <h3>{{nome_do_funcionario}}</h3>
        <hr>
        <p>{{nome_do_funcionario}}</p>
        <small>{{data_do_serviço}}</small>
        <small>{{horario}}</small>
    </div>
</main>
{{else}}
    <h3>Não há agendadamentos</h3>
{{/each}}